import Link from "next/link";
import { apiDatasets } from "@/lib/developer-api";
const code="overflow-x-auto rounded-xl bg-[#101a2d] p-4 font-mono text-xs leading-6 text-slate-100";
export default function DeveloperDocs(){return <main className="min-h-screen bg-[#fbfaf6] text-[#142038]"><header className="sticky top-0 z-20 border-b bg-white/95 backdrop-blur"><div className="mx-auto flex min-h-16 max-w-6xl items-center justify-between gap-4 px-5"><Link href="/" className="font-black tracking-tight">SIME <span className="text-[#d49300]">Developers</span></Link><nav className="flex items-center gap-4 text-sm font-bold"><a href="#authentication">Authentication</a><a href="#endpoints">Endpoints</a><a href="/api/openapi" className="rounded-lg bg-[#e7a000] px-4 py-2 text-white">OpenAPI JSON</a></nav></div></header><div className="mx-auto grid max-w-6xl gap-10 px-5 py-12 lg:grid-cols-[230px_1fr]"><aside className="hidden lg:block"><nav aria-label="API documentation" className="sticky top-24 space-y-2 text-sm"><a className="block rounded-lg px-3 py-2 hover:bg-white" href="#overview">Overview</a><a className="block rounded-lg px-3 py-2 hover:bg-white" href="#authentication">Authentication</a><a className="block rounded-lg px-3 py-2 hover:bg-white" href="#datasets">Datasets</a><a className="block rounded-lg px-3 py-2 hover:bg-white" href="#endpoints">Endpoints</a><a className="block rounded-lg px-3 py-2 hover:bg-white" href="#errors">Errors & limits</a></nav></aside><article className="min-w-0 space-y-12"><section id="overview"><p className="text-xs font-extrabold uppercase tracking-[.2em] text-[#ca8a00]">API reference · v1</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">Connect your school systems to SIME.</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-600">The SIME Integration API provides tenant-isolated access to licensed school data. It is intended for secure server-to-server integrations, reporting pipelines, mobile backends, and approved automation.</p><div className="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950"><b>Never put an API key in browser or mobile application code.</b> Store it in a server-side secret manager. Keys are visible only once when generated.</div></section><section id="authentication" className="space-y-4"><h2 className="text-2xl font-black">Authentication</h2><p className="leading-7 text-slate-600">A SIME super-admin enables the API for a licensed school and generates a read or read/write key. Send the key using <code>X-API-Key</code> (recommended) or a Bearer token.</p><pre className={code}><code>{`export SIME_API_KEY="sime_live_..."\nexport BASE_URL="${process.env.NEXT_PUBLIC_APP_URL||"http://127.0.0.1:6969"}/api/v1"\n\ncurl "$BASE_URL/data/students?limit=25" \\\n  -H "X-API-Key: $SIME_API_KEY"`}</code></pre><p className="text-sm text-slate-500">Keys are school-bound, scope-bound, revocable, optionally expiring, rate-limited, and disabled automatically with the school license.</p></section><section id="datasets"><h2 className="text-2xl font-black">Available datasets</h2><div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{apiDatasets.map(item=><code key={item} className="rounded-lg border bg-white px-3 py-2 text-sm">{item}</code>)}</div><p className="mt-4 text-sm leading-6 text-slate-500">Sensitive user credentials, API keys, AI secrets, and platform audit internals are never returned by the integration API.</p></section><section id="endpoints" className="space-y-8"><h2 className="text-2xl font-black">Endpoints</h2><div className="rounded-2xl border bg-white p-6"><p className="text-xs font-black text-emerald-700">GET</p><h3 className="mt-1 font-mono text-lg font-bold">/api/v1/data/{`{dataset}`}</h3><p className="mt-3 text-sm leading-6 text-slate-600">Lists tenant records. Supports <code>q</code>, <code>limit</code> (1–100), and <code>offset</code> (0–10,000). Requires the <code>read</code> scope.</p><pre className={`${code} mt-4`}><code>{`curl "$BASE_URL/data/attendance?q=8-A&limit=50&offset=0" \\\n  -H "X-API-Key: $SIME_API_KEY"`}</code></pre><pre className={`${code} mt-3`}><code>{`{"data":[{"id":12,"student":"Jessica Rose","class":"8-A","status":"Present"}],"meta":{"dataset":"attendance","limit":50,"offset":0,"count":1}}`}</code></pre></div><div className="rounded-2xl border bg-white p-6"><p className="text-xs font-black text-[#ca8700]">POST</p><h3 className="mt-1 font-mono text-lg font-bold">/api/v1/data/{`{dataset}`}</h3><p className="mt-3 text-sm leading-6 text-slate-600">Creates a validated record. Requires the <code>write</code> scope and school write policy. The JSON fields follow the selected dataset’s fields in SIME.</p><pre className={`${code} mt-4`}><code>{`curl -X POST "$BASE_URL/data/students" \\\n  -H "X-API-Key: $SIME_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{"studentId":"SIME-2042","name":"Noor Ahmed","email":"noor@example.edu","grade":8,"className":"8-A","gender":"female"}'`}</code></pre><pre className={`${code} mt-3`}><code>{`{"data":{"id":42,"studentId":"SIME-2042","name":"Noor Ahmed","email":"noor@example.edu","grade":8,"className":"8-A","gender":"female"}}`}</code></pre></div><div className="rounded-2xl border bg-white p-6"><h3 className="font-bold">Resource request bodies</h3><p className="mt-2 text-sm leading-6 text-slate-600">Examples: assignments use <code>subject, class, teacher, dueDate</code>; attendance uses <code>student, class, date, status, note</code>; results use <code>subject, student, score, teacher, class, date, type</code>. Retrieve the complete machine-readable contract from the OpenAPI document.</p></div></section><section id="errors"><h2 className="text-2xl font-black">Errors, quotas, and versioning</h2><div className="mt-5 overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-[#fbfaf6]"><tr><th className="p-3">Status</th><th>Meaning</th><th>Resolution</th></tr></thead><tbody>{[[400,"Validation failed","Check fields and data types"],[401,"Invalid key","Replace, rotate, or reactivate the key"],[403,"Feature/scope denied","Ask the platform owner to enable access"],[404,"Dataset or related record missing","Verify the dataset and tenant data"],[409,"Duplicate or license limit","Use a unique identifier or raise capacity"],[429,"Rate limit exceeded","Retry after the indicated interval"]].map(row=><tr className="border-t" key={String(row[0])}><td className="p-3 font-bold">{row[0]}</td><td>{row[1]}</td><td>{row[2]}</td></tr>)}</tbody></table></div><p className="mt-4 text-sm leading-6 text-slate-500">All endpoints use JSON and versioned paths. Dates use ISO 8601. Successful list responses include pagination metadata. Breaking changes will use a new path such as <code>/api/v2</code>.</p></section><footer className="border-t pt-8 text-sm text-slate-500">Need access? Contact your SIME platform administrator. You can also execute broader API test suites with TestMu AI HyperExecute.</footer></article></div></main>}
