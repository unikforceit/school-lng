import Link from "next/link";
export default function PersonDetail({kind,name,identifier,fields,related}:{kind:"Student"|"Teacher";name:string;identifier:string;fields:Array<[string,string|number]>;related:Array<{type:string;title:string;href:string}>}){
 const midpoint=Math.ceil(fields.length/2),groups=[fields.slice(0,midpoint),fields.slice(midpoint)];
 return <div className="space-y-5"><header><p className="text-xs font-bold uppercase tracking-[.16em] text-[#c88700]">{kind} directory</p><h1 className="mt-1 text-2xl font-extrabold text-[#102039]">{kind} Details</h1></header><section className="rounded-xl border border-[#efede7] bg-white p-5 shadow-[0_8px_28px_rgba(15,23,42,.03)]"><div className="flex items-center justify-between"><h2 className="font-extrabold text-[#102039]">About Me</h2><span className="text-lg text-slate-400">⋮</span></div><div className="mt-4 grid gap-4 xl:grid-cols-2">{groups.map((group,index)=><article key={index} className="rounded-xl border border-slate-200 p-5"><div className="mb-5 flex items-center gap-3">{index===0&&<div className="flex h-16 w-16 items-center justify-center rounded-full bg-[#fff1c6] text-xl font-extrabold text-[#8c6200]">{name.split(" ").map(part=>part[0]).slice(0,2).join("")}</div>}<div><h3 className="font-extrabold text-[#102039]">{index===0?name:"Contact Information"}</h3><p className="text-xs text-slate-400">{index===0?`${kind} · ${identifier}`:"Verified school record"}</p></div></div><dl className="grid gap-x-5 gap-y-4 sm:grid-cols-2">{group.map(([label,value])=><div key={label}><dt className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">{label}</dt><dd className="mt-1 break-words text-sm font-bold text-[#27364b]">{value||"—"}</dd></div>)}</dl></article>)}</div></section><section className="rounded-xl border border-[#efede7] bg-white p-5 shadow-[0_8px_28px_rgba(15,23,42,.03)]"><h2 className="font-extrabold text-[#102039]">Related school records</h2><p className="mt-1 text-sm text-slate-500">Reusable records connected to this profile.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{related.map((item,index)=><Link key={`${item.type}-${item.title}-${index}`} href={item.href} className="rounded-xl border border-slate-100 p-4 transition hover:border-[#efa900] hover:bg-[#fffaf0]"><span className="text-xs font-bold uppercase text-[#c88700]">{item.type}</span><strong className="mt-1 block text-sm">{item.title}</strong></Link>)}{!related.length&&<p className="col-span-full rounded-xl bg-[#fbfaf7] p-6 text-center text-sm text-slate-400">No related records found yet.</p>}</div></section></div>
}
