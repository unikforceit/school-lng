"use client";
import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
type School={id:string;name:string;active:number;plan:string;licenseStatus:string;licenseExpiresAt:string|null;users:number;students:number;resources:number;maxStudents:number};
type Activity={id:number;actorEmail:string;action:string;tenantId:string|null;createdAt:string};
type Data={totals:{schools:number;activeSchools:number;users:number;students:number;licensedSchools:number;licenseAttention:number};schools:School[];growth:Array<{month:string;schools:number}>;activity:Activity[]};
const icon:Record<string,string>={Schools:"/class.png",Students:"/student.png",Users:"/teacher.png",Licenses:"/result.png"};
export default function SuperAdminDashboard(){
 const [data,setData]=useState<Data|null>(null),[error,setError]=useState("");
 const load=useCallback(async()=>{setError("");try{const response=await fetch("/api/platform/overview");const payload=await response.json().catch(()=>null);if(!response.ok)throw new Error(payload?.error||"Unable to load platform overview");setData(payload.data)}catch(cause){setError(cause instanceof Error?cause.message:"Unable to load platform overview")}},[]);
 useEffect(()=>{void load()},[load]);
 if(error)return <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-6 text-red-700"><h1 className="font-extrabold">Platform dashboard unavailable</h1><p className="mt-1 text-sm">{error}</p></div>;
 if(!data)return <div className="rounded-xl bg-white p-10 text-center text-sm text-slate-400">Loading live platform data…</div>;
 const activePercent=data.totals.schools?Math.round(data.totals.activeSchools/data.totals.schools*100):0;
 const metrics=[["Schools",data.totals.schools,"Total tenants","/superadmin/schools"],["Students",data.totals.students,"Across all schools","/superadmin/schools"],["Users",data.totals.users,"Active school accounts","/superadmin/users"],["Licenses",data.totals.licensedSchools,`${data.totals.licenseAttention} need attention`,"/superadmin/licenses"]] as const;
 return <div className="space-y-5"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#c78a00]">SIME control plane</p><h1 className="mt-1 text-2xl font-extrabold tracking-tight text-[#172033]">Platform overview</h1><p className="mt-1 text-sm text-slate-500">Live school usage, licensing, and operational health.</p></div><Link href="/superadmin/schools" className="inline-flex min-h-10 items-center justify-center rounded-lg bg-[#eaa900] px-4 text-sm font-bold text-white hover:bg-[#ce9400]">Manage schools</Link></header><section className="grid grid-cols-2 gap-3 xl:grid-cols-4">{metrics.map(([label,value,caption,href])=><Link href={href} key={label} className="group rounded-xl border border-slate-100 bg-white p-4 shadow-[0_8px_30px_rgba(15,23,42,.04)] transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-full bg-[#fff7dc]"><img src={icon[label]} alt="" className="h-5 w-5"/></span><span><small className="block text-xs text-slate-500">{label}</small><strong className="block text-2xl font-extrabold text-[#172033]">{value.toLocaleString()}</strong></span></div><p className="mt-3 text-xs text-slate-400 group-hover:text-[#a16f00]">{caption}</p></Link>)}</section><section className="grid gap-4 xl:grid-cols-[.75fr_1.35fr]"><article className="rounded-xl border border-slate-100 bg-white p-5 shadow-[0_8px_30px_rgba(15,23,42,.04)]"><div className="flex items-start justify-between"><div><h2 className="font-extrabold">School availability</h2><p className="text-xs text-slate-400">Enabled tenant instances</p></div><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">Live</span></div><div className="mt-6 flex items-center justify-center"><div role="img" aria-label={`${activePercent}% schools active`} className="relative flex h-44 w-44 items-center justify-center rounded-full" style={{background:`conic-gradient(#eaa900 0 ${activePercent}%,#8a1f17 ${activePercent}% 100%)`}}><div className="flex h-32 w-32 flex-col items-center justify-center rounded-full bg-white"><strong className="text-3xl font-extrabold">{activePercent}%</strong><span className="text-xs text-slate-400">active</span></div></div></div><div className="mt-5 flex justify-center gap-6 text-xs"><span><i className="mr-2 inline-block h-2 w-2 rounded-full bg-[#eaa900]"/>Active {data.totals.activeSchools}</span><span><i className="mr-2 inline-block h-2 w-2 rounded-full bg-[#8a1f17]"/>Disabled {data.totals.schools-data.totals.activeSchools}</span></div></article><article className="rounded-xl border border-slate-100 bg-white p-5 shadow-[0_8px_30px_rgba(15,23,42,.04)]"><div className="flex items-start justify-between"><div><h2 className="font-extrabold">Tenant growth</h2><p className="text-xs text-slate-400">New school instances by month</p></div><Link className="text-xs font-bold text-[#a16f00]" href="/superadmin/schools">View schools</Link></div><div className="mt-7 flex h-52 items-end gap-4 border-b border-l border-slate-200 px-4 pt-4">{(data.growth.length?data.growth:[{month:"No data",schools:0}]).map((point,index)=>{const max=Math.max(...data.growth.map(x=>x.schools),1);const height=Math.max(point.schools/max*85,5);return <div key={`${point.month}-${index}`} className="flex h-full min-w-0 flex-1 flex-col justify-end"><div className="group relative mx-auto w-full max-w-16 rounded-t-md bg-gradient-to-t from-[#ce9000] to-[#ffc843] transition hover:brightness-105" style={{height:`${height}%`}}><span className="absolute -top-6 left-1/2 -translate-x-1/2 text-xs font-bold">{point.schools}</span></div><span className="mt-2 truncate text-center text-[10px] text-slate-400">{point.month}</span></div>})}</div></article></section><section className="grid gap-4 xl:grid-cols-[1.35fr_.75fr]"><article className="rounded-xl border border-slate-100 bg-white p-5 shadow-[0_8px_30px_rgba(15,23,42,.04)]"><div className="flex justify-between"><div><h2 className="font-extrabold">School license health</h2><p className="text-xs text-slate-400">Capacity and renewal overview</p></div><Link href="/superadmin/licenses" className="text-xs font-bold text-[#a16f00]">All licenses</Link></div><div className="mt-4 divide-y">{data.schools.slice(0,6).map(s=>{const percent=Math.min(100,Math.round(s.students/s.maxStudents*100));return <Link href={`/superadmin/schools/${s.id}`} key={s.id} className="grid items-center gap-3 py-3 text-sm sm:grid-cols-[1.2fr_.65fr_1fr]"><span><b>{s.name}</b><small className="block text-slate-400">{s.id}</small></span><span className="capitalize text-slate-600">{s.plan} · {s.licenseStatus}</span><span><span className="mb-1 flex justify-between text-[10px] text-slate-400"><i>{s.students}/{s.maxStudents} students</i><i>{percent}%</i></span><span className="block h-2 overflow-hidden rounded-full bg-slate-100"><i className="block h-full rounded-full bg-[#eaa900]" style={{width:`${percent}%`}}/></span></span></Link>})}</div></article><aside className="rounded-xl border border-slate-100 bg-white p-5 shadow-[0_8px_30px_rgba(15,23,42,.04)]"><div className="flex justify-between"><h2 className="font-extrabold">Recent activity</h2><Link href="/superadmin/audit" className="text-xs font-bold text-[#a16f00]">Audit log</Link></div><div className="mt-3 divide-y">{data.activity.map(a=><div key={a.id} className="py-3 text-sm"><b className="block">{a.action.replaceAll("_"," ")}</b><span className="text-xs text-slate-400">{a.tenantId||"Platform"} · {new Date(a.createdAt).toLocaleString()}</span></div>)}{!data.activity.length&&<p className="py-8 text-center text-sm text-slate-400">Actions will appear here.</p>}</div></aside></section></div>
}
